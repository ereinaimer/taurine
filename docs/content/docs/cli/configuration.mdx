---
title: Configuration
description: View and change Taurine settings from the command line.
---

## List Current Settings

```bash
taurine config list
taurine config ls
```

## Set A Value

```bash
taurine config set start_on_boot true
taurine config set inline_ai_trigger_mode Asymmetric
```

## Reset A Value

```bash
taurine config reset instant_expand
```

Reset everything:

```bash
taurine config reset --all
```

## Common Settings

| Setting | Default | Description |
| :--- | :--- | :--- |
| `pause_hotkey` | ``Alt + ` `` | Global hotkey to pause/resume the Taurine engine. |
| `pause_notifications_enabled` | `false` | Whether to show system notifications when pausing. |
| `start_on_boot` | `true` | Enables/disables automatic startup on login. |
| `system_tray_enabled` | `true` | Show or hide the system tray icon when running. |
| `spinner_style` | `braille` | Style of the thinking spinner (`braille`, `arc`, `classic`). |
| `ai_provider` | - | The default AI provider for Inline AI commands. |
| `ai_model` | - | The default model for the selected AI provider. |
| `ai_custom_endpoint` | - | Custom OpenAI-compatible URL for the `custom` provider. |
| `inline_ai_trigger_mode` | `Symmetric` | The behavior mode (`Symmetric` or `Asymmetric`) for inline AI triggers. |
| `inline_ai_trigger_open` | `>>` | The opening trigger text boundary for inline AI prompts. |
| `inline_ai_trigger_close` | `<<` | The closing trigger text boundary for inline AI prompts. |
| `inline_tab_completion_enabled` | `true` | Enables or disables inline `Tab` / `Shift+Tab` completion. |
| `rpc_mode` | `socket` | RPC transport mode (`socket` using Unix domain sockets / Windows named pipes, or `tcp`). |
| `rpc_host` | `127.0.0.1` | Network interface address the service binds to. |
| `rpc_port` | `50051` | Port the service listens on for RPC. |

## Helpful Aliases

You can use these shorter aliases when running `taurine config set`:

| Alias | Real Setting |
| :--- | :--- |
| `hotkey` | `pause_hotkey` |
| `notifications` | `pause_notifications_enabled` |
| `boot` | `start_on_boot` |
| `system_tray` | `system_tray_enabled` |
| `tray` | `system_tray_enabled` |
| `spinner` | `spinner_style` |
| `delimiter_mode` | `inline_ai_trigger_mode` |
| `open_delimiter` | `inline_ai_trigger_open` |
| `close_delimiter` | `inline_ai_trigger_close` |
| `endpoint` | `ai_custom_endpoint` |
| `custom_endpoint` | `ai_custom_endpoint` |
| `inline_tab_completion` | `inline_tab_completion_enabled` |
| `mode` | `rpc_mode` |
| `host` | `rpc_host` |
| `port` | `rpc_port` |

<Callout title="Startup setting" type="info">
  Changing `start_on_boot` also tries to update the OS startup hook right away, so the setting and
  the actual startup behavior stay in sync.
</Callout>
